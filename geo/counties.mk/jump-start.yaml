---
description: |
  A Makefile that creates a counties geojson file using my [ideal project structure](). Usage:

  ```make
  # Makefile
  include counties.mk

  all: data/build/counties.geojson
  ```
defaultDir: analysis
mainFile: counties.mk
